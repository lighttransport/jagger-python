[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11>=2.10.0",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 140

[project]
name = "jagger"
version = "0.1.9"
readme = {file = "README.md", content-type = "text/markdown"}


[project.scripts]
jagger = "jagger:main"
